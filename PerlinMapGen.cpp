// Generate 
float perlin(float x, float y)


/**
 * Generate a N x M heightmap with height in range [H, K]
 * 
 * This will produce one octave
 */
void perlinMapGen(int N, int M, int K, int H, float lacunarity, float persistance, float** noise) {
    // Step 1: Initialize the grid
    noise = malloc();
}